import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Notices = ({ navigateToScreen }) => {
    return (
        <View>
            <Text>Notices</Text>
        </View>
    );
};

export default Notices;

