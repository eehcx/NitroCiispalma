import { StyleSheet, TextInput, View, Text, TouchableOpacity } from 'react-native';

export default CalcList = ({}) => (

    return (
        <>
            <View>

            </View>
        </>
    );
);