module.exports = {
    dependencies: {
        'react-native-vector-icons': {
            platforms: {
            ios: null, // null indica que utilizará la configuración por defecto
            android: null, // null indica que utilizará la configuración por defecto
            },
        },
    },
};